// used to show area
export interface AreaDTO {
    id: number,
    name: string,
    color: string
}

// used to create area
export interface registerAreaDTO {
    name: string,
    color: string
}

// used to update area
export interface updateAreaDTO {
    name: string,
    color: string
}